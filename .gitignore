# ── Credentials & private config (NEVER commit) ──
cion-config.php
.env
*.env
*.key
*.pem

# ── Lead logs (contain patient data — NEVER commit) ──
leads.log
errors.log
*.log

# ── Hostinger zips / temp download files ──
*.zip
site-baseline/

# ── Windows / editor cruft ──
Thumbs.db
desktop.ini
.DS_Store
.vscode/
*.swp
*~

# ── Node (if tooling added later) ──
node_modules/

# dr-imad (imported PHP site) - never commit PII / secrets / runtime state / deploy-state
dr-imad/api/leads.csv
dr-imad/api/leads.log
dr-imad/api/errors.log
dr-imad/api/recent-phones.json
dr-imad/api/rate-limit.json
dr-imad/.ftp-deploy-sync-state.json
dr-imad/submit.php
dr-imad/api/submit.php

# dr-imad: broad globs (his site has api/ folders in multiple sections)
dr-imad/**/submit.php
dr-imad/**/leads.csv
dr-imad/**/leads.log
dr-imad/**/errors.log
dr-imad/**/rate-limit.json
dr-imad/**/recent-phones.json

# Scheduled-task audit logs (local only, never deploy)
automation-runs/

# Local-only (editor/agent config + local data templates)
.claude/
_data/
